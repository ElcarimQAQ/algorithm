#include<bits/stdc++.h>
#include <algorithm>
using namespace std;
const int maxn=5e5+7;
typedef long long ll;
const int mod=1e9+7;
#define pi acos(-1)
#define INF 0x3f3f3f3f

bool dfs()
{


}

int main ()
{
	cin.tie(0),ios::sync_with_stdio(false);
    int n,m,i,j;
    while(cin >> n >> m){
        if(n == 0 && m == 0)
            break;
        cin >> i >> j;
        
    }
    system.out("pause");
	return 0;
}
