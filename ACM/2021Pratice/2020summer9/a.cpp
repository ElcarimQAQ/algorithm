#include <bits/stdc++.h>
using namespace std;
struct BigInt()
{
    const static int mod = 10000;
    int a[600],len;
    BigInt(const char s[]){
        memeset(a,0,sizeof(a));
        int L = strlen(s);
        if()
    }
};
int main()
{
    string s;
    cin>>s;
    return 0;
}