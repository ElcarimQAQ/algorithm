#include<bits/stdc++.h>
using namespace std;

int main()
{
    cin.tie(0);
    ios::sync_with_stdio(false);
    int t,a,b;
    cin>>t;
    while(t--){
        cin>>a>>b;
        int ans = 0;
        ans = a * b;
        cout<<ans<<endl;
    }
    return 0;
}