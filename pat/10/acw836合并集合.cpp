#include <bits/stdc++.h>
using namespace std;
const int maxn = 10005; 
vector<int> g[maxn];
int vis[maxn],nodeH[maxn];
int maxh ,H = 0;
bool flag = true;
int pre[maxn];
set<int> Count;

int main()
{
    std :: ios::sync_with_stdio(false);
    int n,m;
    
    return 0;
}